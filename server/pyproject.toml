[project]
name = "cvantage-server"
version = "0.1.0"
description = "CVantage backend service"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling>=1.24.2"]
build-backend = "hatchling.build"

[tool.uv]
package = false
